# AEGIS T1 Step8 Training Authorization 鈥?Corrected Runner Compatibility

This compatibility authorization is bound to corrected runner Git blob $RunnerBlob.

It preserves the frozen nine-run Step8 training authorization semantics. The runner requires schema token AEGIS_T1_STEP8_TRAINING_AUTHORIZATION_V1.

Training is authorized only under the frozen protocol. Formal evaluation and official-test access are not authorized.
